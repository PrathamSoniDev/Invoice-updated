# Resend API credentials — copy this file to .env and fill in real values.
# Never commit the real .env file. API keys must stay in the backend only.

# Resend API key (https://resend.com/api-keys)
RESEND_API_KEY=re_your_api_key_here

# Verified sending domain on Resend
RESEND_DOMAIN=selltechindproductions.in

# Port the Express server listens on
PORT=4000
